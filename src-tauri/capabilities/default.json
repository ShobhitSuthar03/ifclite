{
  "identifier": "default",
  "description": "Main window can call NativeBridge host commands and the file dialog.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:webview:allow-create-webview-window",
    "dialog:default"
  ]
}
